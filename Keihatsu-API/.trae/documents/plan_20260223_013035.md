I have identified the two issues and their fixes:

1.  **Incorrect Item Count**: The category tabs are currently displaying the total library count (`offlineLibrary.library.length`) instead of the count for each specific category. I will update the `TabBar` to call `offlineLibrary.getLibraryForCategory(cat).length` for each tab.
2.  **Unresponsive Filter Sheet**: The filter sheet becomes unresponsive because it doesn't rebuild when the provider state changes. The `StatefulBuilder` only handles local state, but the filter toggles update the global provider state. I will replace the `StatefulBuilder` with `Consumer<AuthProvider>` and `Consumer<OfflineLibraryProvider>` to ensure the sheet rebuilds correctly when settings change.

**Plan:**

1.  **Update `LibraryScreen.dart`**:
    *   Fix the item count logic in the `TabBar`.
    *   Refactor `_showDisplaySettings` to use `Consumer` widgets instead of `StatefulBuilder` for the tabs (`_buildFilterTab`, `_buildSortTab`, `_buildDisplayTab`). This ensures the UI updates immediately when a toggle is pressed.

