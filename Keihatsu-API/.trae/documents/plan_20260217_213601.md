I have confirmed that **ManhuaTop is protected by Cloudflare**, which blocks standard requests (returning 403 Forbidden), explaining why the API returns empty results despite a "200 OK" status (the API catches the error and returns empty).

To resolve this as a senior engineer, we need to implement a robust scraping solution using a headless browser, as you suggested.

**Plan:**

1.  **Install Puppeteer**:
    *   This is required to bypass Cloudflare and render dynamic content.
    *   *Note: This installation downloads Chromium and may take a minute.*

2.  **Create `PuppeteerService`**:
    *   A dedicated service to manage a headless Chrome instance.
    *   It will handle page navigation, waiting for content to load (bypassing "Just a moment..." screens), and extracting HTML.

3.  **Update `ManhuaTop` Source**:
    *   Refactor `ManhuaTopSource` to use `PuppeteerService` for fetching pages instead of the default HTTP client.
    *   This will allow us to "see URLs as they come on the web" and parse the fully rendered HTML.

4.  **Fix `WeebCentral` Source**:
    *   The current implementation picks up a "Random" link (likely a navigation button) because the selector is too broad.
    *   I will refine the selector to exclude `random` links and use Puppeteer here as well if needed to ensure accurate rendering.

**Verification**:
*   I will verify the fix by running the `curl` commands again, which should now return the actual list of mangas.
