I will fix the category assignment and display issues by updating two services:

1. **Fix Display Issue**: In `LibraryService`, I will update the `findAll` method to include category data in the response. This ensures the frontend receives the correct category information for each manga, preventing them from appearing as "Default" or in the wrong categories.
2. **Fix Assignment Logic**: In `CategoriesService`, I will update `addMangaToCategory` to use `set` instead of `connect`. This will replace any existing categories with the new one, effectively "moving" the manga to the target category and ensuring it doesn't remain in "Default" (or multiple categories if not intended).

**Files to modify:**

* `src/library/library.service.ts`

* `src/categories/categories.service.ts`

