I will address the keyboard visibility issue and integrate the comments API with upvote/downvote functionality.

### **Plan Overview**

#### **1. Backend Changes (NestJS + Prisma)**
*   **Database Schema (`schema.prisma`)**:
    *   Add `upvotes` and `downvotes` fields to the `Comment` model.
    *   Create a `CommentVote` model to track user votes (prevent duplicate voting).
*   **Service (`comments.service.ts`)**:
    *   Implement `voteComment(userId, commentId, voteType)` logic (toggle votes, switch votes).
    *   Update `findAll` to include the current user's vote status (so the UI knows if *you* liked it).
*   **Controller (`comments.controller.ts`)**:
    *   Add `POST /comments/:id/vote` endpoint.

#### **2. Frontend Changes (Flutter)**
*   **Keyboard Fix (`Comments.dart`)**:
    *   Add `Padding` with `MediaQuery.of(context).viewInsets.bottom` to the bottom sheet container to push the input field above the keyboard.
*   **API Integration**:
    *   Create `CommentsProvider` to manage state (fetching, posting, voting).
    *   Update `CommentsBottomSheet` to:
        *   Fetch real comments from the backend.
        *   Display the **number of replies**.
        *   Show **upvote count** (hide downvote count).
        *   Implement **Like/Dislike buttons** connected to the API.

### **Step-by-Step Implementation**

1.  **Backend**: Update `schema.prisma` and run migrations.
2.  **Backend**: Update `CommentsService` and `CommentsController` to handle votes.
3.  **Frontend**: Fix the keyboard overlap issue in `Comments.dart`.
4.  **Frontend**: Create `CommentsProvider` and integrate endpoints.
5.  **Frontend**: Connect `Comments.dart` UI to the new provider.
