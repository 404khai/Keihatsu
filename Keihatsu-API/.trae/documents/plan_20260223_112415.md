I will implement the history functionality across the Flutter app and NestJS backend, ensuring offline support and synchronization.

### **1. Backend (NestJS)**
*   **Schema Update**: Add `HistoryEntry` model to `prisma/schema.prisma` to track reading progress (`page`, `lastReadAt`).
*   **New Module**: Create `HistoryModule` (`controller`, `service`) to handle:
    *   `GET /history`: Fetch user's reading history.
    *   `POST /history/sync`: Sync reading progress from the app.

### **2. Frontend (Flutter)**
*   **Repository Update (`MangaRepository`)**:
    *   Add `updateReadingProgress(LocalManga manga, String chapterId, int pageIndex)` method.
    *   This method will:
        *   Update `LocalManga` (`lastReadAt`).
        *   Update `LocalChapter` (`lastReadAt`, `lastPageRead`).
        *   Trigger a background sync to the backend (or queue it if offline via `SyncManager`).
*   **Manga Reader Hook (`MangaReaderScreen`)**:
    *   Call `updateReadingProgress` when the page changes (debounced to avoid database spam).
    *   Restore reading position when opening a chapter.
*   **History Screen Implementation (`HistoryScreen`)**:
    *   Replace static data with a `StreamBuilder` watching `Isar` database.
    *   Query `LocalManga` sorted by `lastReadAt` descending.
    *   Fetch the specific "last read chapter" for each manga to display chapter number/time.
    *   **Features**:
        *   **Add to Library**: Toggle `LocalLibraryEntry`.
        *   **Delete**: Clear `lastReadAt` from the local record (hiding it from history).

### **3. Sync & Offline**
*   **SyncManager**: Add handling for `UPDATE_HISTORY` operations to ensure progress made offline is pushed to the server when connectivity is restored.

**Verification**:
I will verify by navigating to the reader, reading a few pages, checking `HistoryScreen` for the entry, and testing the "Delete" and "Add to Library" buttons.
