I will fix the issues with category filtering, management, and sync implementation as follows:

### 1. Fix Category Filtering in `LibraryScreen.dart`
The "Default" category currently shows all manga because the library list is not filtered. I will implement proper filtering to ensure:
- **Default Category**: Shows only manga that have **no** category assignments.
- **Other Categories**: Shows only manga assigned to that specific category.

**Changes:**
- Update `OfflineLibraryProvider` to add a helper method `getLibraryForCategory(String categoryName)`.
  - If `categoryName == "Default"`, return entries with no assignments.
  - Otherwise, return entries assigned to the matching category ID.
- Update `LibraryScreen.dart` to use this method when building `TabBarView` children, instead of passing the full library to every tab.

### 2. Fix Category Management in `LibraryRepository.dart` & `OfflineLibraryProvider.dart`
The current implementation of delete/update relies on `serverId`, which is null for offline-created categories that haven't synced yet. This causes actions to fail.

**Changes:**
- Update `deleteCategory` and `updateCategory` in both `OfflineLibraryProvider` and `LibraryRepository` to accept `int localId` (internal database ID) instead of just `String serverId`.
- **Delete Logic**:
  - Delete the `LocalCategory` by `localId`.
  - Delete all `LocalCategoryAssignment`s linked to this `localId`.
  - If `serverId` exists, queue a `DELETE_CATEGORY` sync operation.
- **Update Logic**:
  - Update the `LocalCategory` name by `localId`.
  - Mark `isSynced = false`.
  - If `serverId` exists, queue an `UPDATE_CATEGORY` sync operation.

### 3. Update `LibrarySettingsScreen.dart`
- Update `_showEditCategoryDialog` to call the updated `updateCategory` method using `category.id` (local ID).
- Update `_showDeleteConfirmDialog` to call the updated `deleteCategory` method using `category.id` (local ID).
- Remove the check `if (category.serverId != null)` to allow deleting/updating categories that haven't synced yet.

### 4. Verify Sync Logic
- The `SyncManager` already handles `CREATE_CATEGORY` (which updates `serverId`) and `ASSIGN_CATEGORY` (which waits for `serverId`).
- My changes to `LibraryRepository` ensure that local operations work immediately, and sync operations are queued correctly only when appropriate (i.e., when a server ID is available to reference).

### 5. API Check
- I have verified `SyncManager` handles `ADD_LIBRARY`, `REMOVE_LIBRARY`, `CREATE_CATEGORY`, `ASSIGN_CATEGORY`, etc.
- The `Default` category logic ("Uncategorized") aligns with `LibraryRepository.addToLibrary` (which ignores "Default" string, resulting in no assignments).

This plan ensures full control over manga categorization and fixes the bugs in local category management.