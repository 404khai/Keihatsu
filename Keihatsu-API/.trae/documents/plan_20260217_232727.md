I have analyzed the local implementation and the external Keiyoushi extensions.

### **Findings**

1. **ManhuaTop**:

   * **Issue**: The current URL `https://manhuatop.org/manga-genre/top-view/page/${page}/` returns a **404 Not Found**. The site structure has likely changed or this specific endpoint was removed.

   * **Fix**: Update the URL to the standard Madara "Popular" endpoint: `https://manhuatop.org/manga/?m_orderby=views`. The CSS selector `.page-item-detail` is standard for this theme and should continue to work.

2. **WeebCentral**:

   * **Issue**: The implementation returns an empty list. My analysis suggests the selector `a[href*="/series/"]:not([href*="random"])` is either too restrictive or the page content is not loading as expected (possibly due to Cloudflare or DOM structure changes).

   * **Fix**:

     * Update the selector to `a[href*="/series/"]` to ensure we capture the links seen in search results (e.g., `/series/01J...`).

     * Improve the parsing logic to robustly extract titles and thumbnails, handling cases where they might be nested differently.

     * Ensure `PuppeteerService` waits for the correct element (`.text-white` or `img` inside the link) to ensure the content is fully hydrated.

### **Plan**

1. **Modify** **`src/sources/extensions/manhuatop.source.ts`**:

   * Change `getPopularManga` URL to `${this.baseUrl}/manga/?m_orderby=views&page=${page}`.

   * Change `getLatestUpdates` URL to `${this.baseUrl}/manga/?m_orderby=latest&page=${page}` (standardizing).

   * Verify selectors match the standard Madara theme structure.

2. **Modify** **`src/sources/extensions/weebcentral.source.ts`**:

   * Update `getPopularManga` to use a more robust selector strategy.

   * Refine `popularMangaParse` to extract details even if the DOM structure varies slightly (e.g., checking for `img` `src` vs `data-src`).

   * Update `searchManga` and `getLatestUpdates` to ensure consistency.

3. **Verification**:

   * After applying the fixes, you can run the `curl` commands again to verify that `ManhuaTop` and `WeebCentral` return a list of manga instead of an empty array.

