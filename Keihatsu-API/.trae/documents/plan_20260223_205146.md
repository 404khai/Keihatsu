**1. Backend Schema Update (Prisma)**

* Modify `HistoryEntry` in `prisma/schema.prisma` to add:

  * `isBookmarked Boolean @default(false)`

  * `isRead Boolean @default(false)`

* Run migration to update the database schema.

**2. Local Database Schema Update (Flutter)**

* Modify `LocalChapter` in `lib/models/local_models.dart` to add:

  * `bool isBookmarked = false;`

  * `bool isRead = false;`

* Run `dart run build_runner build` to regenerate Isar adapters.

**3. Repository & Sync Logic**

* Update `MangaRepository` (and `OfflineLibraryProvider` if applicable) to include:

  * `toggleChapterBookmark(String chapterId, bool value)`

  * `toggleChapterRead(String chapterId, bool value)`

* Ensure these methods update the local Isar database immediately and queue a sync to the backend.

* Update `updateReadingProgress` to automatically mark `isRead = true` when the user reaches the last page.

**4. UI Implementation: MangaDetailsScreen**

* Wrap chapter tiles in a `Dismissible` widget.

  * **Swipe Right**: Toggle Bookmark. Visual: Green background with bookmark icon.

