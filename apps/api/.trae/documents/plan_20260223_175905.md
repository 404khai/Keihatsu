I will implement multi-select functionality and confirmation dialogs in `HistoryScreen.dart` to enhance user control over history deletion.

### **1. State Management & UI Structure**
*   **Hoist `StreamBuilder`**: Move `StreamBuilder` to wrap the `Scaffold` so the `AppBar` can access the current list of history items (needed for "Select All" logic).
*   **Selection State**: Add `Set<int> _selectedIds` to track selected `LocalManga` IDs and `bool _isSelectionMode` to toggle UI states.
*   **Toggle Logic**: Implement `_toggleSelection(int id)`, `_selectAll(List<LocalManga> currentList)`, and `_clearSelection()` methods.

### **2. Interaction Updates**
*   **Single Item Delete**:
    *   Update the trash icon on each row to show a **Confirmation Dialog** ("Delete this from history?") before executing the delete action.
*   **Multi-Select Mode**:
    *   **Trigger**: Long-press on any history item to enter selection mode.
    *   **Visuals**: Highlight selected rows with `brandColor.withOpacity(0.1)` and show a checkbox on the left (or replace the image/avatar with a checkbox, or add a checkbox trailing). I'll follow the previous design pattern: highlight the row and maybe show a checkbox.
    *   **AppBar Changes**:
        *   **Leading**: 'X' button to cancel selection mode.
        *   **Title**: Show "X selected".
        *   **Actions**:
            *   **Select All Checkbox**: A `Checkbox` widget in the app bar to select/deselect all visible items.
            *   **Delete Icon**: A trash icon that triggers a **Confirmation Dialog** ("Delete X items?") before deleting all selected items.

### **3. Implementation Details**
*   **Refactor `HistoryItem`**:
    *   Add `isSelected` and `isSelectionMode` properties.
    *   Add `onLongPress` callback to trigger selection mode.
    *   Add `onTap` callback to toggle selection (if in selection mode) or open manga (if not).
    *   Modify background color based on `isSelected`.
*   **Dialog Reusability**: Create a helper method `_showDeleteConfirmation({required String title, required VoidCallback onConfirm})` to avoid code duplication.

**Verification**:
I will verify the changes by:
1.  Long-pressing an item to enter selection mode.
2.  Selecting multiple items and using "Select All".
3.  Pressing delete and confirming the dialog.
4.  Pressing delete on a single item (normal mode) and confirming the dialog.
