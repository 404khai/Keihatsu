I will now port the three requested extensions from their Kotlin/Android logic into your NestJS backend as TypeScript `HttpSource` classes.

### **Plan of Action**

1.  **Implement `ManhuaTop` Source** (`src/sources/extensions/manhuatop.source.ts`)
    *   **Logic**: Port the "Madara" theme logic used by ManhuaTop.
    *   **Details**: Use Cheerio to parse the HTML structure (`.c-tabs-item__content`, `.post-title`, etc.) from `https://manhuatop.org`.

2.  **Implement `WeebCentral` Source** (`src/sources/extensions/weebcentral.source.ts`)
    *   **Logic**: Port the custom parsing logic for WeebCentral.
    *   **Details**: Handle their specific search API (`/search/data`) and HTML structure (`.text-white`, `img.poster`) from `https://weebcentral.com`.

3.  **Implement `Atsumaru` Source** (`src/sources/extensions/atsumaru.source.ts`)
    *   **Logic**: Port the legacy `Manga Atsumaru` (Niconico) logic.
    *   **Note**: This site (`atsumaru.jp`) has historically had service changes/closures. I will implement the standard port as requested, but add a comment that the upstream site availability may vary.

4.  **Register Sources**
    *   Update `src/sources/sources.service.ts` to import and register these three new classes in the `onModuleInit` method.

5.  **Verification**
    *   I will ensure the new files compile correctly and are integrated into the `SourcesService`.
