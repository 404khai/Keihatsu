I will refactor the codebase to store downloaded manga in a visible `Keihatsu` folder on the device's external storage, while keeping app-internal data (like icons and thumbnails) in private storage.

### **Plan of Action**

1. **Update Dependencies (`pubspec.yaml`)**

   * Add `permission_handler` to manage runtime storage permissions.

2. **Configure Android Permissions (`AndroidManifest.xml`)**

   * Add `READ_EXTERNAL_STORAGE` and `WRITE_EXTERNAL_STORAGE`.

   * Add `MANAGE_EXTERNAL_STORAGE` to ensure reliable access to the root `Keihatsu` folder on Android 11+ devices (necessary for "file manager visibility" at the root level).

3. **Refactor** **`FileService`** **(`file_service.dart`)**

   * Implement a permission request method (`requestStoragePermission`).

   * Create a path resolution strategy that directs:

     * Paths starting with `downloads/` → `/storage/emulated/0/Keihatsu/downloads/` (Public).

     * Other paths (`icons/`, `thumbnails/`) → App's private document directory.

   * Update `downloadFile`, `getChapterPagePath`, and `deleteChapter` to use this new dynamic path resolution.

4. **Update** **`MangaRepository`** **(`manga_repository.dart`)**

   * Ensure permissions are checked before starting a chapter download loop.

   * The existing logic handles absolute paths returned by `FileService`, so it will automatically adapt to the new location for database records.

### **Verification**

* Verify that `FileService` correctly distinguishes between download paths and internal paths.

* Ensure permissions are requested on the first download attempt.

