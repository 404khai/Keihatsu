I will fix the issues with `WeebCentral` and `ManhuaTop` by updating their implementations to match the current website structures and improving error handling.

**Plan:**
1.  **WeebCentral Source Fix**:
    -   Update the base URL for searching/popular manga from `/search/data` (which appears to be invalid or changed) to the standard `/search` page.
    -   Update query parameters (e.g., using `sort=Popularity` instead of `Pop`).
    -   Update HTML selectors to be more robust (e.g., targeting `a[href*="/series/"]` instead of generic classes) to correctly parse manga lists.

2.  **ManhuaTop Source Fix**:
    -   The `500 Internal Server Error` indicates an unhandled exception (likely a parsing error or an upstream 403/404).
    -   I will wrap the request and parsing logic in `try-catch` blocks to handle errors gracefully (returning an empty list or a specific error message instead of crashing the server).
    -   Update the `User-Agent` header to a modern browser string to reduce the chance of being blocked by Cloudflare (which can cause 403/503 errors).

3.  **General Improvements**:
    -   Update the default `User-Agent` in the `HttpSource` abstract class to a modern value.
    -   (Answer to your question): regarding **Keiyoushi**, we should **not** switch to using their extensions directly (as they are written in Kotlin for Android). However, I will use their implementation logic (URLs, selectors) as a reference to ensure our TypeScript implementation is correct and stable.

**Verification**:
-   I will ask you to test the `curl` commands again after the changes to verify the fixes.
