I will fix the ManhuaTop chapter fetching issue by implementing a more robust strategy that handles Madara theme's AJAX loading and improves data parsing.

### **Plan of Action**

1.  **Update `ManhuaTopSource` in `src/sources/extensions/manhuatop.source.ts`**:
    *   **Implement AJAX Fallback:** Modify `getChapterList` to first try the standard page load. If no chapters are found (length is 0), make a POST request to `/wp-admin/admin-ajax.php` with `action: 'manga_get_chapters'` and `manga: <id>`. This is the standard way Madara themes load chapters dynamically.
    *   **Improve Date Parsing:** Add a helper method to handle relative dates like "2 hours ago", "yesterday", "5 days ago", which are common on ManhuaTop. The current `Date.parse()` often fails on these.
    *   **Refine Chapter Number Parsing:** Update the regex to better handle edge cases like "Chapter 10.5" or "Vol.1 Ch.10".
    *   **Puppeteer Optimization:** Ensure Puppeteer waits for the correct selector (`.wp-manga-chapter`) but with a timeout catch to fallback to the AJAX method if the element doesn't appear quickly.

2.  **Verify**:
    *   I will verify the code changes by checking that the new logic compiles and correctly implements the fallback strategy. (Since I cannot run the Flutter app, I will ensure the backend logic covers the known failure modes of Madara sites).

This approach directly addresses the "error loading chapters" by ensuring the backend can find the chapter list even when it's hidden behind an AJAX call or formatted unexpectedly.
