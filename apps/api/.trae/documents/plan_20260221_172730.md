I will implement the requested features by extending the user preferences system and creating a new downloads module.

### 1. Toggle & Pin Extensions
We will use the existing `preferences` JSON field in the `User` model to store extension settings.

**Update User Preferences DTO**
- Modify `src/users/dto/user-preferences.dto.ts` to add `source_preferences`.
- Structure: `Record<string, { enabled: boolean; pinned: boolean }>` (Keyed by source ID).

**Enhance Sources Controller**
- Create `src/auth/guards/optional-jwt-auth.guard.ts` to allow `getSources` to identify users without blocking guests.
- Update `src/sources/sources.controller.ts`:
  - Inject `UsersService`.
  - Apply `OptionalJwtAuthGuard` to `getSources`.
  - Filter out disabled sources and sort pinned sources to the top if a user is logged in.

### 2. Manga Download Feature
I will create a dedicated `DownloadsModule` to handle file operations.

**Create Downloads Module**
- **Service (`src/downloads/downloads.service.ts`)**:
  - Implement `downloadChapter(userId, sourceId, mangaId, chapterId)`.
  - Fetch manga and chapter details via `SourcesService` to get titles for folder naming.
  - Fetch pages via `SourcesService.getPageList`.
  - Save images to `downloads/{extension_name}/{manga_title}/{chapter_title}/`.
  - Handle file sanitization and directory creation.
- **Controller (`src/downloads/downloads.controller.ts`)**:
  - Endpoint: `POST /downloads/process` (Protected).
  - Body: `{ sourceId, mangaId, chapterId }`.
- **Module (`src/downloads/downloads.module.ts`)**: Register controller and service.

**Integration**
- Register `DownloadsModule` in `src/app.module.ts`.

### Plan Steps
1.  **Create Optional Auth Guard**: Helper for optional user context.
2.  **Update DTO**: Add `source_preferences` to `UpdateUserPreferencesDto`.
3.  **Update Sources Controller**: Implement filtering and sorting logic in `getSources`.
4.  **Create Downloads Module**: Implement service and controller for downloading chapters.
5.  **Register Module**: Add to `AppModule`.
