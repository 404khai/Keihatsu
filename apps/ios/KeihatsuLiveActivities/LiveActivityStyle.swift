import SwiftUI
import UIKit

extension Color {
    static let keihatsuActivityAccent = Color(
        red: 244.0 / 255.0,
        green: 203.0 / 255.0,
        blue: 0
    )
    static let keihatsuActivityBackground = Color(uiColor: .systemBackground)
}
